// src/reducer.ts
export const gameReducer = (state: any, action: any): any => {
  switch (action.type) {
    case 'UPDATE_RESOURCES':
      // TODO: Implement resource update logic
      return state;
    default:
      return state;
  }
};